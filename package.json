{
  "name": "diybugcontrol-theme",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shopify theme for DIY Bug Control",
  "scripts": {
    "check": "node scripts/theme-check.mjs",
    "seed": "node --env-file-if-exists=.env shopify/seed.mjs",
    "seed:dry": "node --env-file-if-exists=.env shopify/seed.mjs --dry-run",
    "package": "node scripts/package.mjs",
    "seed:definitions": "node --env-file-if-exists=.env shopify/seed.mjs --definitions-only"
  },
  "devDependencies": {
    "@shopify/theme-check-node": "^3.28.1"
  }
}
